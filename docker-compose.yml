version: '3.8'

services:
  redis:
    image: redis:alpine
    container_name: weather_redis
    network_mode: "host"
    restart: always

  mongodb: 
    image: mongo:7.0
    container_name: weather_mongo
    ports:
      - "127.0.0.1:27017:27017"
    volumes: 
      - mongo_data:/data/db
    restart: always
    
volumes:
  mongo_data: